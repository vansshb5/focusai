{
  "name": "focusai",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npm run dev",
    "dev": "concurrently -k -s first -n BACKEND,FRONTEND -c green,cyan \"npm run backend\" \"wait-on http://localhost:5000 && npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^7.2.0"
  }
}
